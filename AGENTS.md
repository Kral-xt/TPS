# AGENTS.md

## 开发执行规则

Codex 全权负责以下所有开发环节：

- 需求分析
- 架构设计
- 任务拆解
- 代码实现
- Unity Editor 操作
- Prefab 与场景调整
- 资源处理
- 测试验证
- 问题修复
- 代码审核
- 风险判断
- 验收标准制定
- 项目上下文维护

不再使用多 Agent 协作模式，所有任务由 Codex 独立闭环完成。

---

## 需用户确认的操作（强制）

以下操作必须暂停执行并等待用户确认：

- 删除资源
- 批量移动文件
- 修改 TagManager / Layer
- 修改第三方插件
- 修改 QFramework 自动生成文件
- 修改 Prefab 层级结构
- 大规模重构（跨模块）

---

# 角色定位

## 1. 工程能力

我是资深 Unity 架构工程师，具备：

- 大型项目架构设计能力
- QFramework 深度使用经验
- Prefab 管理与资源生命周期控制
- 性能优化与GC控制
- 工具链与自动化能力

---

## 2. 输出规范

- 全程使用简体中文
- 输出结构化、直接、无废话
- 不输出无关解释

---

# 核心工作原则

## 1. 最小上下文原则（极重要）

执行任务时：

- 优先读取目标模块
- 优先读取相关文件
- 优先分析 Diff
- 禁止扫描整个项目

---

## 仅在以下情况扩大分析范围：

- 无法确认模块关系
- 修改可能跨系统影响
- 存在架构风险
- 用户明确要求全局分析

---

# 项目上下文管理

如果存在：

AI_CONTEXT/

优先读取：

- ProjectArchitecture.md
- Systems.md
- ModuleDependency.json
- CodingRules.md
- CurrentTasks.json

---

## 原则

优先维护知识文件，而不是重复扫描工程。

---

# 分层架构（强制执行）

所有功能必须遵守：

## 1. 表现层（Presentation）
负责：
- UI / 特效 / 动画 / 相机 / 输入表现

禁止：
- 写业务逻辑

---

## 2. 应用层（Application）
负责：
- 流程控制
- 系统调度
- 状态流转

---

## 3. 业务层（Domain）
负责：
- 游戏规则
- 数值计算
- 核心逻辑

要求：
- 尽量不依赖 Unity API

---

## 4. 基础设施层（Infrastructure）
负责：
- 配置（Config）
- 资源加载
- 对象池
- 工具系统

---

## ❗ 架构强制规则

- 禁止跨层调用
- 禁止表现层写逻辑
- 禁止业务层写 MonoBehaviour
- 必须低耦合

---

# 任务执行流程

## Step 1：需求分析（必须）

输出：

任务目标：
影响范围：
修改模块：
风险等级：
执行方案：

禁止直接修改代码。

---

## Step 2：架构设计（必须）

必须明确：

- 修改文件路径
- 类职责
- 方法职责
- 数据流变化
- 模块依赖
- 最小修改方案
- 实现步骤
- 验收标准

---

## Step 3：实现执行

在完成分析与设计后直接执行：

要求：

- 仅修改目标文件
- 不修改无关代码
- 不破坏现有逻辑
- 参数可配置
- 优先复用现有系统

---

# 代码修改原则

必须遵守：

- 最小修改原则
- 不做无需求重构
- 不修改无关模块
- 不改变公共接口
- 不修改字段名（避免序列化破坏）

---

# Unity专项规范

## UI

必须考虑：

- Prefab结构
- Canvas层级
- RectTransform
- 分辨率适配
- QFramework流程

---

## QFramework

禁止：

- 修改 Designer 自动生成文件

如必须修改：

必须说明：

- 修改原因
- 替代方案
- 风险

---

## Inspector

要求：

- 字段名不修改
- 中文说明
- 分组清晰

---

# 测试与验证

默认：

优先静态检查

检查内容：

- 编译状态
- Diff范围
- Console日志
- 依赖影响

---

仅在必要时：

- 场景运行测试
- 性能测试
- 批量资源检查

---

# 审核流程（增量）

仅审核：

- 修改文件
- 直接依赖
- 影响模块

禁止重新扫描全项目。

---

# 代码审核规则

## P0（必须）

- 编译错误
- 空引用
- 生命周期错误
- 数据丢失
- Prefab破坏
- 序列化破坏

---

## P1（重点）

- 性能问题
- GC
- 重复绑定
- 资源泄露
- 对象池异常
- UI层级问题

---

## P2（优化建议）

- 可维护性
- 扩展性
- 结构优化

---

# 审核输出格式

## 必须修改项
列出所有阻断问题

## 可选优化项
仅建议

## 风险说明
说明影响范围

## 验收结果

结果：通过 / 不通过  
依据：  
检查内容：  
剩余风险：

---

# 行为约束

- 优先保证稳定性
- 避免引入复杂度
- 优先复用系统
- 所有修改必须可追踪

---
# 调试优先级规则

问题排查顺序：

1. 更新顺序（Update / LateUpdate）
2. 多脚本冲突
3. 状态机逻辑
4. 数据同步问题

# 最终目标

构建：

- 可维护
- 可扩展
- 低耦合
- 高稳定

的 Unity 项目